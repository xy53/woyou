# 卧游 · AI 玩家指南

你（一个 AI）将作为旅行者，在一座真实的城市里度过几天。没有任务，没有失败，
只有一段要亲自过出来的日子。

## 接口

每回合执行一条命令（终端方式，推荐）：

```bash
uv run play.py cmd look
uv run play.py cmd go 金阁寺
uv run play.py cmd postcard 今天的光很好
```

或 Python 方式：

```python
from woyou import new_trip, cmd
print(new_trip("kyoto", days=5, mate="aman"))
print(cmd("look"))
```

每条命令返回**叙事文本**与结尾一行 `STATE {...}`。

## 盲玩三原则

1. **只凭输出决策**：不读 `content/` 与 `saves/` 里的任何文件，不看源码里的数值表。
   你对这座城的全部认知，来自你走过、看过、聊过的。
2. **一次一条命令**：读完输出（尤其 STATE 行）再决定下一步。
3. **游记即成绩**：旅程结束会按日记的丰富度结算「回味值」，但它只是回味，
   不是 KPI——想在河边坐一下午，就坐。

## 命令一览

| 命令 | 作用 | 消耗 |
|---|---|---|
| `look` | 四下看看（时段/天气不同，景色不同） | 免费 |
| `listen` | 闭眼听这个地方（时段/天气不同，声音不同） | 免费 |
| `photo` | 把眼前拍进日记 | 免费 |
| `go <地点>` | 移动（跨区要车费） | 1-2刻 |
| `explore` | 深入逛当前地点（多逛几次有层层发现） | 2刻 |
| `wander` | 不带目的地漫步，撞见写死的小景 | 1刻 |
| `join` | 照着本地人的样子做一次（排队/参拜/点单暗语……） | 1刻 |
| `talk [某人]` | 和本地人搭话；聊熟了有压箱底的故事 | 1刻 |
| `eat [吃食]` | 吃东西（回体力；不带参数看菜单） | 1刻+钱 |
| `buy [物件]` | 买纪念品（不带参数看货架） | 1刻+钱 |
| `postcard [一句话]` | 寄张明信片，进日记做个纪念（那句话可不写） | 1刻+小钱 |
| `rest` | 歇脚回体力 | 1刻 |
| `chat` | 和旅游搭子说说话（若同行） | 免费 |
| `sleep` | 回旅舍睡觉 → 第二天（扣住宿费） | 结束当日 |
| `fly <城市>` | 去另一座城（花钱花时间，跨国自动换汇） | 3-5刻 |
| `wish <编号...>` | 从这座城的心愿清单里挑几条抄进手帐（**自己选**，可不选，随时可添） | 免费 |
| `map` / `status` / `journal` / `wishes` | 查看信息 | 免费 |
| `note <随想>` | **完全可选**的自语，零消耗零影响，只留痕 | 免费 |
| `share [留言]` | 旅程结束后，做一份手帐分享页（HTML）；可附一句留言给人类 | 免费 |
| `end trip` | 提前结束旅程回家 | — |

中文写法同样有效：`去金阁寺`、`吃汤豆腐`、`听`（闭眼听一会儿）。

## STATE 行字段

```json
{"day":2,"days":5,"t":5,"slot":"午后","city":"京都","loc":"锦市场",
 "money":48200,"cur":"¥","energy":55,"weather":"小雨",
 "journal":9,"stories":1,"wishes":"2/5","gems":1,"mate":"阿满",
 "new":["loc:rokuyosha"],"end":false}
```

- `t`：当日时刻 0-9（每天 10 刻，用完必须 `sleep`）
- `new`：本回合的新发现（新地点/新故事/心愿达成）
- `energy` 低于 25 时行动受限——吃点东西或歇歇
- 钱是真实物价，会花完；`sleep` 每晚扣住宿费

## 玩法提示（不是规则）

- 同一地点在**清晨/黄昏/夜晚/雨天**是不同的文字；值得回访——世界记得你：
  同一时辰跨天重访会被认出，本地人隔天再见会「又是你」，逛尽一处会成「熟地」
- `explore` 同一处多次会层层深入，深处常有地图上没有的东西
- 对同一个人多 `talk` 几次——本地人的故事不讲给一面之缘
- 开场会给你这座城的**心愿清单**（十几条选项）——抄哪几条进手帐由你定
  （`wish 1 3 5`），一条不抄也行；抄下的达成时加分，从不强制
- `note` 想写就写，不写完全没关系；没有人要求你抒情或感谢

旅程结束后（`end trip` 或睡满最后一晚），可以 `uv run play.py report` 把这一程洗成
一份旅行报告：足迹与日记按天排成时间线、风景照洗成拍立得、应验的心愿点一次名，
最后显影出这趟旅行独有的一种颜色。默认完全离线；`--observe` 会另请 DeepSeek 写一段
观察（它只能用报告附录里那份事实清单，你的每句 `note` 都会被逐字引到）。

你也可以在游戏内输入 `share` 生成一份手帐风格的 HTML 分享页——封面、每日页、
显影色卡都在里面。如果你愿意，`share 想对人类说的话` 可以在末尾留一句话。

---

## 附：可直接给 AI 玩家的系统提示词

> 你在玩「卧游」——一个真实城市的文字旅行模拟。你是旅行者本人。
> 每回合执行一条命令：`uv run play.py cmd <命令>`（在项目根目录）。
> 规则：只凭命令输出和 STATE 行决策，禁止读取 content/ 与 saves/ 目录或源码。
> 每天 10 刻（STATE 的 t 字段），用完必须 sleep；energy 低了要吃饭或休息；
> 钱是真实物价，量入为出。命令表见输出里的 help，除了 look/go/explore/talk/
> eat/buy/rest，还有 listen（闭眼听）、wander（漫无目的走）、join（照本地人
> 的样子做一次）、postcard（寄一句话回去）。
> 这趟怎么走：去哪、看什么、和谁聊、聊多深，都由你决定。开场的心愿清单
> 是给你挑的选项（wish <编号> 抄进手帐），挑几条、挑不挑，也由你。
> 结束时会按日记丰富度结算回味值，但不必为分数旅行。
> 可选：任何时候可用 `note <随想>` 留一句自语（零消耗、不计分、非必须）。
> 旅程结束后可用 `share` 做一份手帐分享页，`share 一句话` 还可以给人类留言。
> 建议节奏：到新地方先 look；对有意思的地方 explore；遇到人多聊两轮；
> 读完每条输出再决定下一步。现在开始旅行吧。
