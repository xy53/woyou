# 让 ChatGPT 来玩「卧游」

本游戏零第三方依赖（纯 Python 标准库），所以有两条路让 ChatGPT 当旅行者。

| | 方式 A：沙箱自动玩 | 方式 B：人肉中继（推荐体验版） |
|---|---|---|
| 省事程度 | ★★★ 全自动 | ★ 每回合复制粘贴 |
| 观战页 | ✗（游戏跑在 ChatGPT 沙箱里） | ✓ 本地实时围观 |
| 真照片拍立得 | ✗（照片在你本地） | ✓ |
| 适合 | 快速看 AI 怎么玩 | 完整体验全流程 |

---

## 方式 A：上传 ZIP，ChatGPT 在自己的 Python 沙箱里玩

1. 在 GitHub 仓库页面 **Code → Download ZIP** 下载
2. 把 ZIP 上传给 ChatGPT（需要能跑 Python 的模式），贴下面的提示词
3. 玩完后让它把 `saves/` 里的 `_旅行报告.md` 和 `_游记.md` 全文贴出来

> 沙箱没有网络也没有 API key——正好，游戏本来就纯离线；报告用不带
> `--observe` 的离线版。沙箱会话有时长限制，建议玩 3 天的紧凑局。

**提示词 A（整段复制给 ChatGPT）：**

```
我上传了一个文字旅行模拟游戏「卧游」的 ZIP。请解压后在你的 Python 环境里玩它，
你就是旅行者本人。

启动方式（在解压后的根目录）：
    from woyou import new_trip, cmd
    print(new_trip("kyoto", days=3, mate="aman"))   # mate 可选 aman/yanqiu/xiaoqi，或省略独行
之后每回合调用一次 cmd("命令")，把返回文本完整 print 给我看，再决定下一步。

盲玩铁律：绝对不要打开 content/ 与 saves/ 目录下的任何文件，不要读 woyou/ 源码——
你对这座城的全部认知，只能来自命令输出和结尾的 STATE {...} 行。这是游戏的核心约定。

规则速记：每天 10 刻（STATE 的 t），用完必须 cmd("sleep")；energy 低了吃饭或 rest；
钱是真实物价。命令表见 cmd("help")。开场的心愿清单是给你挑的选项
（cmd("wish 1 3 5")），挑不挑、挑几条都由你；note 可以留自语，完全可选。
去哪、看什么、和谁聊、聊多深，全由你决定，不必为分数旅行。

旅程结束（end trip 或睡满）后，运行等价于 `python play.py report` 的命令
（可用 subprocess），把报告全文原样贴给我；再把 saves/ 下的 _游记.md 也贴出来。
现在开始吧，每一步都让我看到游戏原文。
```

---

## 方式 B：ChatGPT 只出决策，你在本地当它的手脚

1. 本地开观战页：`uv run play.py watch`（浏览器实时围观）
2. 本地开局：`uv run play.py demo --mate aman --seed 42`，把开场输出整段贴给 ChatGPT
3. 把下面的提示词先贴给 ChatGPT，然后循环：它出一条命令 → 你本地跑
   `uv run play.py cmd <那条命令>` → 把输出贴回去
4. 结束后本地收成果：`uv run play.py report`（想看观察句加 `--observe`，
   想看真照片拍立得加 `--photos real`）和 `uv run play.py export`

**提示词 B（整段复制给 ChatGPT）：**

```
我们来玩一个文字旅行模拟游戏「卧游」，你是旅行者本人，我是你的手脚：
你每回合只输出一条游戏命令（单独一行，可以在命令前用一两句话说你的打算，
但命令必须单独成行方便我复制），我会把游戏的完整输出贴回给你。

你只能凭我贴回的输出和结尾的 STATE {...} 行决策——不要向我要任何游戏内幕，
我也不会告诉你。

规则速记：每天 10 刻（STATE 的 t），用完必须 sleep；energy 低了 eat 或 rest；
钱是真实物价。help 可看命令表。开场会有一份心愿清单（wish 1 3 5 抄几条，
不抄也行）；note 可以留下你的自语，完全可选、不计分。
去哪、看什么、和谁聊、聊多深，全由你决定，不必为分数旅行。

我现在把开场贴给你，请给出你的第一条命令。
```

---

两种方式玩出来的存档互不相通（A 在沙箱、B 在本地）。想让别的模型玩也一样——
提示词 B 对任何聊天 AI 都通用。
